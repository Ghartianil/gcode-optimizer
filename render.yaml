services:
  - type: web
    name: gcode-optimizer
    env: python
    buildCommand: ./build.sh
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
    pythonVersion: 3.10